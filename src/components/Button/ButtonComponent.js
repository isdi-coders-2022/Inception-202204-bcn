import ButtonStyles from "./ButtonComponentStyles";

const Button = (iconName, iconPath) => {
  return (
    <Button>
      {/* <img
            title={iconName}
            className="icon"
            src={iconPath}
            alt={iconName}
          /> */}
    </Button>
  );
};

export default Button;
